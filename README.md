# Sound Bot

web interface to play audio on a virtual audio device which loops back to a microphone.

## Usage

```bash
curl -X POST http://localhost:5000/api/play/Yippie.ogg

pw-cat -p --target="TeamSpeak" sounds/Yippie.ogg
```
